<!--pages/dict/dict.wxml-->
<view class="dict-container">
  <view class="search-header">
    <view class="search-box">
      <input 
        class="search-input"
        placeholder="输入英文或中文查词..."
        value="{{searchText}}"
        bindinput="onSearchInput"
        bindconfirm="onSearch"
        confirm-type="search"
      />
      <button class="search-btn" bindtap="onSearch" disabled="{{isSearching}}">
        <text wx:if="{{!isSearching}}">🔍</text>
        <text wx:else>⏳</text>
      </button>
    </view>
  </view>

  <scroll-view class="dict-content" scroll-y="true">
    <view class="search-results" wx:if="{{searchResults.length > 0}}">
      <view class="result-item" wx:for="{{searchResults}}" wx:key="word">
        <view class="word-header">
          <text class="word-text">{{item.word}}</text>
          <text class="word-phonetic" wx:if="{{item.phonetic}}">{{item.phonetic}}</text>
        </view>
        <text class="word-definition">{{item.definition || item.translation}}</text>
      </view>
    </view>

    <view class="recent-searches" wx:if="{{recentSearches.length > 0 && searchResults.length === 0}}">
      <text class="section-title">最近搜索</text>
      <view class="recent-list">
        <button 
          class="recent-item" 
          wx:for="{{recentSearches}}" 
          wx:key="*this"
          bindtap="onRecentTap"
          data-word="{{item}}"
        >
          {{item}}
        </button>
      </view>
    </view>
  </scroll-view>
</view>